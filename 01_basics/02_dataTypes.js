"use strict" //treat all code as new new version

// alert( 3 + 3 ) we are using nodejs ,not browser

console.log("data types");

let name = "Mujtaba"
let age = 18    
let isloggedIN = false

// number range =>2 to power 52 or 53
// bigint 
// string => ""
// boolena = false/true
// null => standalone value , null is reprenstation of empty value
// undefine => means value not assigned
// symbol => unique

// object

console.table([typeof(null), typeof(undefined), typeof (name), typeof(age)])

// console.table([typeof(null), typeof(undefined), typeof(name), typeof(age)]);